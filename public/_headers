# Cache hashed assets (JS, CSS, images) FOREVER
# The hash in the filename changes when the file changes, so this is safe
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Cache the HTML shell for 1 hour
/*
  Cache-Control: public, max-age=3600, stale-while-revalidate=86400